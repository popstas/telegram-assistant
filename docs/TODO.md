# TODO

- [x] При ошибке резолвинга `chat:`-правила в `telegram.access` — писать warning и пропускать правило, а не ронять команду
  - **Симптом:** устаревшее правило (`- chat: expertizemeAssistant` в `data/config.yml`) → любая gated-команда падает с `no entity found for reference 'expertizemeAssistant'` и exit 2. Пример: `telegram-assistant messages recent --entity @wyrtensi` не отработал, хотя к самому `@wyrtensi` правило есть и доступ разрешён.
  - **Причина:** `access/service.py` (`_build`, ~строки 270-287) резолвит каждый `chat_refs` через `self._resolver.resolve(ref)`; `EntityNotFoundError`/`AmbiguousEntityError` пробрасывается наружу и валит всю команду.
  - **Что сделать:** ловить ошибки резолва отдельного правила, логировать warning (с текстом ref и причиной) и пропускать только это правило; остальная политика строится как обычно. Продумать fail-safe: пропуск правила сужает права (deny-by-default), а не расширяет — это безопасно, но должно быть заметно в выводе CLI.
  - **Проверить:** `access list` / `access check` при этом отрабатывали успешно — разобраться, почему поведение расходится с `messages recent`, и привести к единому.
  - **Обвязка:** тесты на нерезолвимое `chat:`-правило (команда отрабатывает, warning есть, правило не действует); при необходимости — упоминание в README/SKILL.md.
  - **Сделано 2026-07-29:** `_ensure_index` ловит `EntityError` **по каждому ref** (а не по правилу), пишет warning `access_rule_ref_unresolved` (ref, причина, permissions) и складывает в `Authorizer.unresolved_refs`; `access check` печатает их в поле `unresolved_refs`. `FloodWaitError` по-прежнему пробрасывается — `except` намеренно узкий. Расхождения с `access list` не было: он вообще не строит индекс и резолвер не создаёт, поэтому падал только `access check` (ровно так же, как `messages recent`).
- [ ] Дорезать e2e-обвязку после переезда на Избранное + папку `Клиенты`
  - **Сделано 2026-07-28:** все три скрипта (`scripts/e2e_test.sh`, `e2e_cli_test.sh`, `e2e_http_extras_test.sh`) переведены на `FOLDER=Клиенты`, `TEST_CHAT=me` (Избранное — все message-шаги) и `GROUP_TITLE="e2e test group"` (создаётся самим `e2e_test.sh`; на нём остались только групповые шаги: topics, members, group rename, folder round-trip). Mass send в папку убран из живых прогонов — он веерно бьёт по реальным чатам папки; остаётся под юнит-тестами с фейками. Имя папки в URL перкодируется через `jq @uri` (кириллица). `CLAUDE.md` обновлён: preconditions + запрет запускать мутирующий e2e без спроса.
  - **Причина переезда (проверено live):** старый `Client chat test` забанен Telegram (`restricted=True`, `reason='terms'`, `default_banned_rights` до 2038) — любая отправка отдаёт `ChatRestrictedError`, чинить нечего. Плюс под тем же именем висит мёртвый legacy `Chat(id=5257063462, deactivated=True, migrated_to=3915612716)`, отправка в который даёт `PeerIdInvalidError`. Папки `Clients` на аккаунте вообще нет.
  - **Что осталось:** (1) добавить в `data/config.yml` правило `folder: Клиенты` с `permissions: [read, write]` — без него `groups create` падает на WRITE-гейте папки назначения, и весь `e2e_test.sh` встаёт на третьем шаге; (2) прогнать все три скрипта живьём **по явному запросу** и убрать оставшиеся расхождения; (3) решить, что делать с `USER_TO_ADD/@popstas` — если это и есть технический аккаунт, `members bulk-add` самого себя отработает вхолостую.
  - **Осторожно:** аккаунт уже блокировали за подозрительную активность, полный e2e — это десятки отправок/пересылок/реакций за минуты. Не запускать по собственной инициативе.
- [x] rich_markdown: анимированный `.gif` не прикрепляется — нужна конвертация в mp4
  - **Контекст:** `.gif` объявлен как video (`tg://video?id=…`), но его mime — `image/gif`, а не `video/*`, поэтому `utils.get_attributes()` не выдаёт `DocumentAttributeVideo` (проверено локально). Это ровно та же дыра, из-за которой `.mp3` не резолвился по `tg://audio` без `DocumentAttributeAudio`. По аналогии `_document_attributes()` сейчас добавляет `DocumentAttributeAnimated()`.
  - **Факт (от пользователя):** анимированный gif в статью так не прикрепляется — чтобы он ушёл, файл надо конвертировать в **mp4** (Telegram и в обычных чатах хранит «гифки» как немой mp4-документ с `DocumentAttributeAnimated`).
  - **Что сделать:** решить, где живёт конвертация — (а) конвертировать `.gif` → mp4 на нашей стороне при аплоаде (ffmpeg как опциональная зависимость, иначе внятная ошибка), либо (б) не конвертировать, а отклонять `.gif` с сообщением «сконвертируйте в mp4» ещё в `media_kind()`/`scan_media`, до открытия operation-строки. Зафиксировать выбранное поведение в CLAUDE.md рядом с остальными media-фактами, обновить SKILL.md (+ ресинк) и README, добавить тесты.
  - **Сделано 2026-07-29:** выбран вариант (а) — `messages/media_probe.py::convert_gif_to_mp4()` конвертирует `.gif` в mp4 через `ffmpeg` перед аплоадом (temp-файл удаляется в `finally`, имя пишется назад как `<исходное имя>.mp4`); без `ffmpeg` на PATH `.gif` отклоняется `_validate_rich_files` до открытия operation-строки. Диагноз уточнён спайком `scripts/spike_rich_gif.py` (2026-07-29, Избранное, msg 407434-407437): дело не только в том, что `utils.get_attributes()` не выдаёт `DocumentAttributeVideo` — сам Telegram-сервер транскодирует **маленький** (98 КБ) `image/gif` в mp4 независимо от присланных атрибутов, а **крупный** (21.2 МБ) остаётся `image/gif` без `DocumentAttributeAnimated`/`thumbs`, и статья падает с `RICH_MESSAGE_VIDEO_INVALID`. Порог недокументирован, поэтому конвертируем всегда, а не только выше порога.
- [x] rich_markdown: крупные видео уходят с заглушечными атрибутами и рендерятся пустыми
  - **Симптом (live, 2026-07-29, Избранное, msg 407429/407430):** в статье с 10 видео три показываются пустым прямоугольником — `Вид с веранды` (16.73 МБ), `На велике с горки до дома` (12.72 МБ) и безымянное `b567c535…` (17.19 МБ). Медиа при этом на месте: `messages.getRichMessage` отдаёт все 34 блока и все 10 документов нужного размера.
  - **Причина (проверено):** `hachoir` не установлен, поэтому `utils.get_attributes()` возвращает для **любого** mp4 заглушку `DocumentAttributeVideo(duration=0, w=1, h=1, supports_streaming=False, video_codec=None)` — воспроизведено локально на всех видео заметки, включая те, что в итоге отображаются нормально. `_document_attributes()` (`messages/telethon_backend.py:210`) отдаёт её как есть. Дальше метаданные чинит **сервер**, переразбирая загруженный файл, но делает это не для всех: у семи видео ≤ 6.30 МБ на сервере реальные `duration` (1.7-109.6 с), реальные `w`/`h`, `thumbs=2`, `supports_streaming=True`, `video_codec` h264/hevc — а у трёх ≥ 12.72 МБ так и остаётся `duration=0, w=1, h=1`, `thumbs=None`, `video_codec=None`. Порог серверной обработки где-то между 6.30 и 12.72 МБ. Клиент, получив `1×1` без длительности и без превью, рисует пустоту.
  - **Что сделать:** не полагаться на серверную починку — заполнять `DocumentAttributeVideo` самим. `ffprobe`/`ffmpeg` в системе есть (`/usr/bin/ffprobe`), на проблемном файле выдаёт `hevc, 854×480, 73.68 s`. Варианты: (а) добавить `hachoir` в зависимости (тот путь, который Telethon ожидает сам, но для mp4 он частичный); (б) свой `_probe_video()` на `ffprobe` с `supports_streaming=True` и превью через `ffmpeg -ss ... -frames:v 1` в `thumb=`; (в) сузить до крупных файлов, если не хотим ffmpeg как зависимость. Решить, что делать, когда пробер недоступен: слать заглушку как сейчас (и предупреждать для файлов > ~6 МБ) или отклонять.
  - **Смежное:** тот же корень, что у пункта про `.gif` ниже (`DocumentAttributeAnimated`) и у `.mp3` (`DocumentAttributeAudio(duration=0)`) — везде затыкаем дыру `get_attributes()` вручную. Стоит сделать один общий путь заполнения атрибутов, а не третий частный случай.
  - **Обвязка:** тесты на `_document_attributes()` с фейковым пробером (видео получает реальные duration/w/h, аудио и gif не ломаются), CLAUDE.md рядом с остальными media-фактами, README/SKILL.md если появится новая зависимость или флаг.
  - **Сделано 2026-07-29:** выбран вариант (б) на `ffprobe` — `messages/media_probe.py::probe_media()` (без импортов Telethon) читает duration/width/height/has_video/has_audio, `_document_attributes()` в `messages/telethon_backend.py` строит единый video/audio/animation-атрибут из пробера (заменяя заглушку Telethon, а не дополняя её) — это тот самый «один общий путь» вместо трёх частных случаев, и он же закрывает смежный пункт про `.gif` выше. Видео дополнительно получает сгенерированный `ffmpeg`-превью-кадр (`extract_thumbnail()`, 10% от длительности) как `thumb=` — это и есть то, чего не хватало для пустого прямоугольника. `ffprobe`/`ffmpeg` — опциональные бинарники, не pip-зависимость: неудачный пробер не ошибка, уходит прежняя заглушка с `WARNING` в логе. Каждый вызов пробера/конвертации — блокирующий subprocess через `asyncio.to_thread`.
- [ ] rich_markdown: задокументировать `part=True` и добавить post-send верификацию медиа
  - **Повод (live, 2026-07-29, Избранное):** после отправки статьи с 34 медиа в клиенте не отображались два тяжёлых mp4 — выглядело как тихий дроп медиа при `operation_status: completed`. **Проверено — дропа нет.**
  - **Факт (проверено read-back'ом обеих отправок, msg 407429 и 407430):** `Message.rich_message` в самом сообщении приходит с **`part=True`** — это усечённое превью статьи (в нашем случае 109-113 блоков из 262 и 18 медиа из 34). Полное тело тянется отдельным RPC **`messages.getRichMessage(peer, id)`** → `Messages.messages[0].rich_message` с `part=False`. Там всё на месте: 262 блока, 34 медиа (24 `PageBlockPhoto` + 10 `PageBlockVideo`), включая все крупные mp4 (16 730 478 / 17 193 447 / 12 721 469 байт). Размерного порога для медиа в статье не обнаружено. То есть «пропажа» — ленивая догрузка на стороне клиента, а не потеря на нашей стороне.
  - **Что сделать:** (1) записать оба факта (`part=True` у сообщения, `messages.getRichMessage` для полного тела, отсутствие размерного порога) в CLAUDE.md рядом с остальными media-фактами, проверенными спайками — иначе это будет переоткрываться при каждом «медиа не доехало»; (2) опционально — после отправки статьи с медиа делать read-back через `messages.getRichMessage` и сравнивать число медиа-блоков с `rich_markdown_media`, отдавая warning с именами файлов при расхождении (сейчас surface рапортует `completed` вслепую; лишний RPC на каждую отправку — цена, решить, включать ли по флагу).
  - **Обвязка:** тесты на read-back с недостающим медиа (warning есть, имена файлов названы), SKILL.md (+ ресинк) в списке warnings у `messages send`, README — если верификацию всё-таки включаем.
  - **Заодно:** при отправке статьи с большим числом медиа не обрезать вывод CLI — сохранять полный stderr.
- [x] Добавить `ffmpeg` в Docker-образ
  - **Повод:** в v0.11.0 медиа в rich-статьях зависит от внешних бинарников `ffprobe`/`ffmpeg` (`messages/media_probe.py`): без них крупное видео уходит с заглушечными атрибутами и рендерится **пустым прямоугольником**, а `.gif` отклоняется ещё до открытия operation-строки (конвертация в mp4 обязательна). В образе их нет — `Dockerfile` ставит только `ca-certificates` и `tini`, так что в контейнере обе фичи не работают. Предупреждение об этом стоит в README и в описании релиза https://github.com/popstas/telegram-assistant/releases/tag/v0.11.0.
  - **Что сделать:** добавить в `apt-get install` слой `ffmpeg` (тянет и `ffprobe`); проверить прирост размера образа — `ffmpeg` из Debian весит порядка сотен МБ, при желании рассмотреть `--no-install-recommends` + минимальный набор или отдельный тег образа. Снять предупреждение из README (раздел про Docker) и из CLAUDE.md, если оно там осталось.
  - **Обвязка:** `scripts/docker-smoke.sh` — проверить, что `ffprobe -version` и `ffmpeg -version` доступны внутри контейнера; `tests/test_docker_image.py` при наличии Docker.
  - **Сделано 2026-08-02:** `ffmpeg` добавлен в существующий `apt-get install --no-install-recommends` слой (пакет даёт и `ffprobe`). Замерено на реальных сборках: `python:3.12-slim` 179 МБ → с `ffmpeg` 804 МБ (**+625 МБ**, 205 пакетов — `libavdevice` тянет SDL2/X11); образ проекта 367 МБ → 1 ГБ. Статические бинарники из `mwader/static-ffmpeg` дали бы 552 МБ, но добавили бы зависимость от стороннего образа — выбран apt (официальные пакеты, security-обновления Debian). `docker-smoke.sh` после `/health` проверяет `ffmpeg -version`/`ffprobe -version` внутри контейнера, `tests/test_docker_image.py` сторожит строку в Dockerfile и проверку в скрипте. README/CLAUDE.md/SKILL.md: предупреждение «образ без ffmpeg» заменено на факт, что образ их везёт, с ценой в размере.
- [ ] rich_markdown: пробросить `noautolink` и `rtl` у `InputRichMessageMarkdown`
  - **Контекст:** остаток от пункта про inline media выше. `noautolink` (в Bot API — `skip_entity_detection`) отключает автодетект ссылок/упоминаний/хэштегов, `rtl` переключает направление статьи. Сейчас оба всегда `None`.
  - **Что сделать:** поля в `SendMessageRequest` (по образцу `spaced_paragraphs`: request-level, не backend-kwarg, если возможно — иначе only-when-set `extra`), флаги CLI и поля HTTP/MCP, дефолты в `telegram.defaults`; SKILL.md (+ ресинк), README, тесты.