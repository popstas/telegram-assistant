# TODO

- [ ] Finish rename out-of-band leftovers: rename the GitHub repo / git remote, rename the local repo dir, and regenerate `skills-lock.json` `computedHash` via the skills tool. (Code/docs rename `telegram-planfix-assistant` → `telegram-assistant` already merged.)
- [ ] Make Planfix an optional **plugin**, not a core dependency (`telegram-assistant` is broader than Planfix).
  Decouple Planfix-specific bits from the generic core so the core has **zero Planfix knowledge**; keep current behavior available when the plugin is enabled.
  **Settled design:**
  - **Mechanism:** in-tree **hook protocol**. Core defines a `Plugin` protocol with hook points: `idempotency_anchor`, `first_message`, `post_create_cleanup`, `protected_accounts`, `title_postfix`. One bundled `PlanfixPlugin` implements it; activated by config flag `plugins.planfix.enabled` (default **off**). Core imports nothing from the plugin.
  - **Idempotency anchor:** core renames `planfix_task_id` → generic **`external_ref`** (key shape `group_create:external_ref=…`, `topic_create:external_ref=…`). CLI `--planfix-task-id` and HTTP `planfix_task_id` remain as **backward-compat aliases** mapping onto `external_ref`; add generic `--external-ref` / `external_ref`.
  - **Default / compat:** **hard off, explicit opt-in**. With the plugin off the core still honors `external_ref` idempotency generically, but the `/task <id>` send, `@planfix_bot` welcome/reply cleanup, and `@planfix_bot` protected-account guard only run when `plugins.planfix.enabled: true`. (Breaking change vs. today; acceptable pre-1.0.)
  - **Hook points to move behind the plugin** (from the coupling map): `/task <id>` first-message + `@planfix_bot` cleanup (`groups/service.py`, `topics/service.py`), `PLANFIX_BOT_USERNAME` protection in `members/service.py`, `cleanup_planfix_messages` / `task_reply_wait_seconds` / `group_title_postfix` config (move into `plugins.planfix.*`), default reserve `@planfix_bot` + `main_account_label` default in `config/loader.py` template, and `planfix_task_id` in `observability` log context (becomes generic `external_ref`).
- [ ] Add an integration/agent-setup layer — an `INTEGRATION.md` guide (à la [obsidian-agent-base/INTEGRATION.md](https://github.com/popstas/obsidian-agent-base/blob/main/INTEGRATION.md)) that lets Claude Code wire this assistant into another project.
  Should be an interactive, agent-driven setup: ask one question at a time (with defaults), then scaffold/edit the consuming project's files. Cover: `data/config.yml` (folder name, defaults, postfix, cleanup flags, bearer token), Telethon `auth` session bootstrap, HTTP-vs-CLI choice, Planfix wiring, and a final validation checklist. Keep it in sync with `SKILL.md`/`README.md`.
- [ ] Add read/write access restrictions (access control) so the assistant can only act on permitted chats.
  Config-driven allowlist with two scoping modes: (a) explicit **whitelist of chats/entities**; (b) **folder restriction** — only chats within configured folder(s), with an "any chat in the folder" toggle. **Both reads and writes are gated**, but the grant has a permission level per entry: **read-only** vs **read+write** — read ops (e.g. get-recent-messages) need at least read; mutating ops need write. Enforce in the domain/service layer (not just CLI) across all operations; deny by default; return a clear authorization error (fits the error taxonomy in `docs/init-plan.md`). Decide config shape under `telegram.*` and how it interacts with `default_chat_folder`.
- [ ] Accept a flexible **entity** everywhere instead of numeric `chat_id` (mirror how `telegram-download-chat` takes its first positional arg).
  Add one shared resolver (e.g. `resolve_entity(ref)`) used by all domains (messages, groups, topics, members, folders) and exposed in both CLI args and HTTP bodies. Accept: numeric id (with/without `-100` prefix), `@username`, `t.me/...` and `joinchat/+invite` links, phone, and exact chat **title** (scan dialogs). Resolution order, per `telegram-download-chat` (`src/telegram_download_chat/core/entities.py:172-252`): numeric → try `PeerChannel`/`PeerChat`/`PeerUser` in turn → dialog scan by id → delegate the raw string to Telethon `client.get_entity()` for usernames/links/phones. For titles, scan dialogs; **error on ambiguity** (multiple chats, same title) and on not-found, with clear messages fitting the error taxonomy in `docs/init-plan.md`. Keep numeric `chat_id` working (backward compatible). Cache resolved entities per request. Resolve **then** authorize against the access-restrictions above. Also covers the original "send by @username/title" need.
- [ ] Support sending **media** — photos, videos, and files/documents (today messages are text-only, `messages/service.py:85`).
  Extend the `MessageBackend.send_message` path to accept attachments via Telethon `send_file` (single + albums), with optional caption and caption formatting. Wire through CLI (`messages send` — accept local file path(s) / URL) and the HTTP send endpoint (file upload or URL/base64 — decide). Validate type/size, surface clear errors, and keep plain-text send working. Respect access-restrictions (resolve → authorize) and the entity resolver above.
- [ ] **Scheduled / delayed messages** — send a message at a future time (Telethon `send_message(..., schedule=...)`). Expose `--schedule`/`schedule_at` in CLI + HTTP; accept absolute time or relative delay. Decide how it interacts with the worker queue and idempotency.
- [ ] **Mute / unmute** a contact or chat (toggle notification settings via `UpdateNotifySettings`). CLI + HTTP, by entity; optional mute duration.
- [ ] **Remove** a contact/chat from a folder (add already exists via `add_chat_to_folder`, `folders/service.py`). Add the inverse + make folder membership idempotent; CLI + HTTP, by entity. (Generalizes the access-restrictions folder handling.)
- [ ] **Reactions** — set/remove an emoji reaction on a message (`SendReaction`). CLI + HTTP, by entity + message id + emoji.
- [ ] **Forward** a message from one entity to another (`forward_messages`). CLI + HTTP, source entity + message id(s) → target entity; respect access-restrictions on the target.
- [ ] **Get recent messages** from an entity (default **5**). Promote the existing internal `get_recent_messages` (`groups/telethon_backend.py`) to a first-class read op across all entities; expose in CLI + HTTP with a `limit` (default 5). Returns id, sender, date, text/media summary. A **read** op — requires read-level access per the restrictions above.
- [ ] Add an **HTTP MCP server** exposing the assistant's operations as MCP tools, with **OAuth via Google**.
  Stand up a streamable-HTTP MCP endpoint that maps the domain ops (send/media/forward/reactions/get-recent/groups/topics/members/folders) to MCP tools, reusing the same service layer + access-restrictions (read/write scoping) as the HTTP API. Auth = Google OAuth (decide flow + which Google accounts/domain are allowed → maps to an identity, then to access scope). Decide whether it lives in the existing FastAPI app or a separate process, and how it coexists with the current bearer-token `/telegram/*` auth.
