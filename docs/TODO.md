# TODO

- [ ] При ошибке резолвинга `chat:`-правила в `telegram.access` — писать warning и пропускать правило, а не ронять команду
  - **Симптом:** устаревшее правило (`- chat: expertizemeAssistant` в `data/config.yml`) → любая gated-команда падает с `no entity found for reference 'expertizemeAssistant'` и exit 2. Пример: `telegram-assistant messages recent --entity @wyrtensi` не отработал, хотя к самому `@wyrtensi` правило есть и доступ разрешён.
  - **Причина:** `access/service.py` (`_build`, ~строки 270-287) резолвит каждый `chat_refs` через `self._resolver.resolve(ref)`; `EntityNotFoundError`/`AmbiguousEntityError` пробрасывается наружу и валит всю команду.
  - **Что сделать:** ловить ошибки резолва отдельного правила, логировать warning (с текстом ref и причиной) и пропускать только это правило; остальная политика строится как обычно. Продумать fail-safe: пропуск правила сужает права (deny-by-default), а не расширяет — это безопасно, но должно быть заметно в выводе CLI.
  - **Проверить:** `access list` / `access check` при этом отрабатывали успешно — разобраться, почему поведение расходится с `messages recent`, и привести к единому.
  - **Обвязка:** тесты на нерезолвимое `chat:`-правило (команда отрабатывает, warning есть, правило не действует); при необходимости — упоминание в README/SKILL.md.
- [ ] Починить/заменить e2e-таргет `Client chat test` — любая отправка в него отдаёт `ChatRestrictedError` / 400
  - **Симптом:** `scripts/e2e_test.sh`, `scripts/e2e_cli_test.sh`, `scripts/e2e_http_extras_test.sh` падают на шагах отправки. Проверено в рамках `docs/plans/completed/20260726-rich-message-send.md` (Task 1): ошибка **не** специфична для rich-сообщений — обычный `SendMessageRequest` без `rich_message` в тот же `InputPeerChannel` падает идентично.
  - **Что сделать:** разобраться в причине ограничения (права аккаунта в чате / статус самого чата) либо завести новый тестовый чат в папке `Clients` и обновить preconditions в `CLAUDE.md` и в самих скриптах.
- [ ] Проверить rich-message send с non-Premium аккаунта
  - **Контекст:** спайк подтвердил отправку только с Premium-аккаунта (`id=241225329`, `premium=True`), поэтому Premium-гейт на стороне сервера остаётся непроверенным.
  - **Что сделать:** отправить статью с non-Premium аккаунта; если сервер её отклоняет — зафиксировать код ошибки и описать требование в README (сейчас там про Premium ничего нет).
- [ ] Разгрузить `messages send` в CLI (`cli/main.py`, ~850 строк на одну команду)
  - **Контекст:** rich-обвязка добавила туда ~300 строк инлайна: взаимоисключения флагов, чтение файла с BOM, разбор `--rich-file`/`--media-group`, `scan_media`, предпроверка индексов групп, проверка длины, второй прогон `normalize_rich_markdown` и дюжина тернарников `x if is_rich else None` для dry-run. Локали (`rich_files: tuple[object, ...]`) уже пришлось типизировать как `object`, потому что `RichFile` импортируется лениво внутри функции.
  - **Что сделать:** вынести `_resolve_rich_send_input(...) -> RichSendInput | None` (frozen dataclass `markdown`/`files`/`choices`, все `typer.Exit(code=2)` внутри) и `_rich_dry_run_markers(...)`; тело команды сокращается до одного вызова и одного `if rich_input is not None`. Тесты уже покрывают поведение — рефакторинг должен пройти на них без правок.
- [ ] rich_markdown: проверить `.gif` живой отправкой
  - **Контекст:** `.gif` объявлен как video (`tg://video?id=…`), но его mime — `image/gif`, а не `video/*`, поэтому `utils.get_attributes()` не выдаёт `DocumentAttributeVideo` (проверено локально). Это ровно та же дыра, из-за которой `.mp3` не резолвился по `tg://audio` без `DocumentAttributeAudio`. По аналогии `_document_attributes()` теперь добавляет `DocumentAttributeAnimated()` — но, в отличие от четырёх фактов из спайка, это **не подтверждено живым API**.
  - **Что сделать:** `.venv/bin/python scripts/spike_rich_media.py --file <loop.gif> --entity me`; если сервер отвечает `RICH_MESSAGE_VIDEO_INVALID` — подобрать рабочую форму (например `DocumentAttributeVideo(duration=0, w, h)` вдобавок) и зафиксировать факт в CLAUDE.md рядом с остальными.
- [ ] rich_markdown: пробросить `noautolink` и `rtl` у `InputRichMessageMarkdown`
  - **Контекст:** остаток от пункта про inline media выше. `noautolink` (в Bot API — `skip_entity_detection`) отключает автодетект ссылок/упоминаний/хэштегов, `rtl` переключает направление статьи. Сейчас оба всегда `None`.
  - **Что сделать:** поля в `SendMessageRequest` (по образцу `spaced_paragraphs`: request-level, не backend-kwarg, если возможно — иначе only-when-set `extra`), флаги CLI и поля HTTP/MCP, дефолты в `telegram.defaults`; SKILL.md (+ ресинк), README, тесты.
