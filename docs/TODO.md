# TODO

- [x] Ускорить отправку сообщений при наличии `folder:`-правил в `telegram.access` (сейчас `messages send` идёт ~45–58с вместо пары секунд)
  - **Проблема:** любая отправка/операция с активной access-политикой, где есть хотя бы одно `folder:`-правило, занимает ~45с. Сам send <0.1с — всё время съедает построение карты членства чат→папки.
  - **Причина:** `authorizer.require(chat_id, WRITE)` при наличии folder-правил вызывает `_folder_memberships()` → `folder_backend.list_folders()`. В `folders/telethon_backend.py:71-73` `list_folders()` для **каждого** чата в **каждой** из 26 папок последовательно делает `await client.get_entity(peer)` — сотни сетевых round-trip'ов подряд. Замер: `list_folders() -> 26 folders` = 44.62с; резолв всех chat-правил суммарно ~0.35с.
  - **Вариант A (устранить корень):** для построения membership-карты не нужен `get_entity` — `InputPeer*` из dialog-фильтра уже содержит id (`channel_id`/`chat_id`/`user_id`). Сетевой запрос нужен только ради `title`, который для проверки членства не используется. Сделать в authorizer «дешёвый» путь построения membership, читающий id прямо из peer; `list_folders()` для CLI-инспекции (где title нужен) оставить как есть. → отправка станет ~1с, семантика доступа не меняется.
  - **Вариант C (кэш, дополняет A):** persist-кэш карты членства (чат→папки), чтобы не перечислять папки на каждый вызов. Для CLI один процесс = один вызов, поэтому нужен именно persist-cache (на диске) с инвалидацией. Требует продумывания: где хранить, TTL/инвалидация при изменении папок, консистентность с hot-reload. Полезно и серверу.
  - **Решение:** делать A + C (persist cache). Требует обсуждения деталей C перед реализацией.
- [x] Добавить операцию `messages edit` (редактирование текста/подписи отправленного сообщения) с gate `edit_only_session_messages: true`
  - **Scope:** новая доменная операция по образцу `react`/`forward` — `messages/service.py` (edit-функция с идемпотентностью через `OperationStore`) + адаптер в `messages/telethon_backend.py` (`client.edit_message(chat_id, message_id, text)`, трансляция `FloodWaitError`) + CLI-команда `messages edit` + HTTP-эндпоинт + MCP-инструмент + backend-factory (`edit_backend_factory`, 503 когда клиент не подключён).
  - **Access-гейт:** редактирование — WRITE-операция на резолвнутый чат (как send/react). Плюс новый policy-флаг `edit_only_session_messages` (по аналогии с `delete_only_session_messages`, default **true**): ограничивает `messages edit` сообщениями, которые отправил сам этот процесс (трекинг через существующий `SentMessageRegistry`); `false` — разрешает править произвольные сообщения. Флаг должен переопределяться **per-rule** (`AccessRule.edit_only_session_messages`) с той же логикой специфичности (chat > folder > all > policy default) и restrictive-`true`-wins, что и у delete.
  - **Обвязка:** `--dry-run` для CLI; обновить `AccessConfig`/`AccessRule` в `config/models.py`; обновить `skills/telegram-assistant/SKILL.md` (+ ресинк в `~/.claude/...`), README (Commands + MCP tool catalog), `EXPECTED_TOOL_NAMES` в `tests/test_mcp_mount.py`; тесты по образцу delete-гейта.
  - **Заметка:** Telegram позволяет редактировать только свои сообщения и в пределах окна (обычно 48ч) — учесть в обработке ошибок.
- [x] Добавить операцию `messages pin` / `messages unpin` (закрепить/открепить сообщение в чате или топике)
  - **Scope:** доменный `service` + `telethon_backend` (`client.pin_message` / `client.unpin_message`, либо `messages.updatePinnedMessage`; трансляция `FloodWaitError`) + CLI `messages pin`/`unpin` + HTTP + MCP + backend-factory (503 когда клиент не подключён).
  - **Access-гейт:** WRITE на резолвнутый чат (как send/react). `--dry-run` для CLI.
  - **Опции:** флаги вроде `--silent` (не слать уведомление о закреплении) и `--pm-oneside`; для unpin — конкретный `message_id` либо открепить все.
  - **Обвязка:** обновить SKILL.md (+ ресинк), README (Commands + MCP-каталог), `EXPECTED_TOOL_NAMES` в `tests/test_mcp_mount.py`, тесты по образцу `react`.
- [x] Добавить операцию `messages download` (скачать медиа/документ/голос **из** сообщения в локальный файл)
  - **Scope:** доменный `service` + `telethon_backend` (`client.download_media` по chat_id + message_id → путь/файл; трансляция `FloodWaitError`) + CLI `messages download` (`--out`/`--dir`) + HTTP + MCP + backend-factory. Отличать от существующего `messages/downloads.py`, который качает URL→temp **для отправки**, а не из сообщения.
  - **Access-гейт:** READ на резолвнутый чат (как `recent`).
  - **Заметка:** ограничение размера/типа, обработка сообщений без медиа; для HTTP/MCP продумать отдачу (путь на диске vs base64/поток). `--dry-run` где применимо.
  - **Обвязка:** SKILL.md (+ ресинк), README, `EXPECTED_TOOL_NAMES`, тесты.
- [x] Добавить операцию `messages search` (поиск сообщений по тексту внутри чата)
  - **Scope:** доменный `service` + `telethon_backend` (`messages.search` / `client.iter_messages(search=...)`; трансляция `FloodWaitError`) + CLI `messages search` + HTTP + MCP + backend-factory. Возвращать по образцу `recent` (`RecentMessage`-ряды), newest-first.
  - **Фильтры:** `--query` (обязательно), опционально `--from` (отправитель), `--limit`, окно по времени `--minutes` (как в `recent`), возможно `--topic-id`.
  - **Access-гейт:** READ на резолвнутый чат (как `recent`).
  - **Обвязка:** SKILL.md (+ ресинк), README, `EXPECTED_TOOL_NAMES`, тесты.
- [ ] При ошибке резолвинга `chat:`-правила в `telegram.access` — писать warning и пропускать правило, а не ронять команду
  - **Симптом:** устаревшее правило (`- chat: expertizemeAssistant` в `data/config.yml`) → любая gated-команда падает с `no entity found for reference 'expertizemeAssistant'` и exit 2. Пример: `telegram-assistant messages recent --entity @wyrtensi` не отработал, хотя к самому `@wyrtensi` правило есть и доступ разрешён.
  - **Причина:** `access/service.py` (`_build`, ~строки 270-287) резолвит каждый `chat_refs` через `self._resolver.resolve(ref)`; `EntityNotFoundError`/`AmbiguousEntityError` пробрасывается наружу и валит всю команду.
  - **Что сделать:** ловить ошибки резолва отдельного правила, логировать warning (с текстом ref и причиной) и пропускать только это правило; остальная политика строится как обычно. Продумать fail-safe: пропуск правила сужает права (deny-by-default), а не расширяет — это безопасно, но должно быть заметно в выводе CLI.
  - **Проверить:** `access list` / `access check` при этом отрабатывали успешно — разобраться, почему поведение расходится с `messages recent`, и привести к единому.
  - **Обвязка:** тесты на нерезолвимое `chat:`-правило (команда отрабатывает, warning есть, правило не действует); при необходимости — упоминание в README/SKILL.md.
- [ ] Починить/заменить e2e-таргет `Client chat test` — любая отправка в него отдаёт `ChatRestrictedError` / 400
  - **Симптом:** `scripts/e2e_test.sh`, `scripts/e2e_cli_test.sh`, `scripts/e2e_http_extras_test.sh` падают на шагах отправки. Проверено в рамках `docs/plans/20260726-rich-message-send.md` (Task 1): ошибка **не** специфична для rich-сообщений — обычный `SendMessageRequest` без `rich_message` в тот же `InputPeerChannel` падает идентично.
  - **Что сделать:** разобраться в причине ограничения (права аккаунта в чате / статус самого чата) либо завести новый тестовый чат в папке `Clients` и обновить preconditions в `CLAUDE.md` и в самих скриптах.
- [ ] Проверить rich-message send с non-Premium аккаунта
  - **Контекст:** спайк подтвердил отправку только с Premium-аккаунта (`id=241225329`, `premium=True`), поэтому Premium-гейт на стороне сервера остаётся непроверенным.
  - **Что сделать:** отправить статью с non-Premium аккаунта; если сервер её отклоняет — зафиксировать код ошибки и описать требование в README (сейчас там про Premium ничего нет).
- [ ] rich_markdown: по умолчанию подставлять U+00A0 на место пустых строк между абзацами, добавить опцию `--no-spaced-paragraphs` для отмены
  - **Контекст:** сервер парсит markdown в `PageBlock`-ы и рисует соседние `PageBlockParagraph` вплотную — пустые строки исходника теряются. Проверено вручную: абзац из одного неразрывного пробела (U+00A0) между абзацами даёт видимый отступ.
  - **Что сделать:** нормализацию делать в домене (`messages/service.py`, до валидации длины), чтобы поведение было одинаковым на CLI/HTTP/MCP; вставлять spacer только между двумя обычными абзацами — не после заголовков, не внутри списков/таблиц/цитат/fenced code. Учесть, что spacer'ы увеличивают длину и считаются в лимите `MAX_RICH_MARKDOWN_CHARS`.
  - **Также вставлять spacer перед заголовками** (`##` и остальные уровни) — чтобы секция визуально отделялась от предыдущего абзаца. После заголовка spacer не нужен.
  - **⚠️ Лимит блоков:** кроме 32768 символов Telegram ограничивает статью **500 блоками** (включая вложенные, элементы списков, строки таблиц) — каждый spacer это отдельный `PageBlockParagraph`, т.е. они удваивают счёт абзацев. На отправленной статье вышло ~180 блоков из 500, но для длинных текстов нужен контроль/ошибка до отправки. Источник лимитов: `~/projects/js/telegram-functions-bot/node_modules/@grammyjs/types/rich.d.ts` (docstring `InputRichMessage`): 32768 символов, 500 блоков, 16 уровней вложенности, 50 медиа-вложений, 20 колонок таблицы.
  - **Обвязка:** CLI-флаг `--no-spaced-paragraphs`, эквивалентное поле в HTTP-теле и MCP-инструменте; SKILL.md (+ ресинк), README, тесты (spacer'ы вставлены по умолчанию; с флагом markdown уходит байт-в-байт).
- [ ] rich_markdown: поддержка inline media — photo, video
  - **Контекст:** сейчас медиа в статье возможно только публичным https-URL (`![](https://…jpg "caption")`), локальные файлы вставить нельзя — при отправке обсидиановой статьи все `![[Pasted image ….png]]` / `.mp4` пришлось вырезать.
  - **Нашлось в `~/projects/js/telegram-functions-bot`** (та же фича на Bot API 10.1, `api.sendRichMessage`, `src/telegram/send.ts:139`; спецификация диалекта — docstring `InputRichMessage` в `node_modules/@grammyjs/types/rich.d.ts`):
    - **Bot API умеет медиа только по HTTP/HTTPS URL** — локальный upload в статью там недоступен. Медиа задаётся **только отдельным блоком**: `![](https://…/photo.jpg "Photo caption")`; поддерживаются `.jpg`, `.mp4` (video), `.mp3` (audio), `.ogg` (voice note), `.gif` (animation) — тип определяется по MIME и URL, title после URL = подпись.
    - Группировка: `<tg-collage>` и `<tg-slideshow>` (внутри — обычные markdown-медиаблоки, разделённые пустыми строками); в HTML-варианте есть `<figure>`/`<figcaption>`/`<cite>` и спойлер-атрибут `tg-spoiler`.
    - Лимиты: до **50 медиа-вложений** на статью (photo+video+audio суммарно), 500 блоков, 32768 символов.
    - **Практика из их плана** (`docs/superpowers/specs/2026-07-05-grammy-port-design.md`): markdown с image-URL превращается в media-блок и **требует прав на медиа в чате** — иначе весь send отклоняется; у них на это сделан fallback на легаси-отправку. Нам fallback не нужен (он явно запрещён), но ошибку надо распознавать и объяснять.
  - **Преимущество нашего MTProto-пути:** `InputRichMessageMarkdown(markdown, rtl, noautolink, files: list[InputRichFile])` — есть поле `files`, которого нет в Bot API: `InputRichFilePhoto(id: str, photo: InputPhoto)` / `InputRichFileDocument(id: str, document: InputDocument)`. То есть локальный файл можно залить (`client.upload_file` → `InputPhoto`/`InputDocument`) и сослаться на него из markdown по строковому `id`. **Проверить**: синтаксис ссылки на такой `id` внутри markdown (скорее всего вместо URL в `![]( … )`) — в telethon-стабах не описан, нужен спайк по образцу `scripts/spike_rich_message.py`.
  - **Что сделать:** спайк на `files=` + локальную картинку/видео; затем surface-обвязка (как передавать локальные пути вместе с `--rich-markdown`) и распознавание ошибки «нет прав на медиа в чате».
  - **Группировка подряд идущих медиа:** по умолчанию идущие подряд медиаблоки (без текста между ними) заворачивать в `<tg-collage>`. Обычный случай из обсидиановых заметок — 2-3 картинки подряд; отдельными блоками они растягивают статью.
  - **Диалог в скилле:** если в статье нашлись группы (2+ медиа подряд), `skills/telegram-assistant/SKILL.md` должен перед dry-run спросить через `AskUserQuestion`, нужно ли какие-то группы превратить в слайдшоу или разгруппировать. Если да — опрос по каждой группе отдельным вопросом: «После текста `<последние ~50 символов текста перед медиа>` как сгруппировать медиа?» с вариантами `Collage` / `Slideshow` / `Ungrouped`. При одной-единственной группе и ответе «оставить как есть» — не переспрашивать.
  - **Заодно:** у `InputRichMessageMarkdown` есть `noautolink` (в Bot API это `skip_entity_detection`) — отключает автодетект ссылок/упоминаний/хэштегов; и `rtl`. Сейчас мы не пробрасываем ни то, ни другое.
  - **Обвязка:** SKILL.md (+ ресинк), README, тесты; уточнить взаимодействие с текущим запретом `--rich-markdown` + `--file`/`--file-url`.
